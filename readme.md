удалить `ibus`

```bash
sudo apt purge ibus ibus-*
```

---

установить `feh` для обоев рабочего стола

```bash
sudo apt install feh
```

---

установить `polybar`

```bash
sudo apt install polybar fonts-font-awesome fonts-noto-color-emoji
```

---

установить скриншотер `flameshot`
```bash
sudo apt install flameshot
```